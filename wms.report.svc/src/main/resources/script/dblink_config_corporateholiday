SELECT
    result.id,
    result.corporateholidayid,
    result.corporateholidayphotourl,
    result.esid,
    result.createdby,
    result.createddate,
    result.modifiedby,
    result.modifieddate,
    result.holidaydate,
    result.holidayenddate,
    result.holidayname,
    result.holidaystartdate,
    result.holidaystatus,
    result.holidaytype,
    result.isallday
FROM dblink('dbname=hcis-configuration host=128.199.186.186 port=5432 user=hcis password=snGCcNt0w2JGHQBrP2kaTlQVk7HlxVjy'::text, 'SELECT * FROM mst_corporateholiday'::text) result(id bigint, corporateholidayid character varying(255), corporateholidayphotourl text, esid character varying(255), createdby character varying(255), createddate timestamp without time zone, modifiedby character varying(255), modifieddate timestamp without time zone, holidaydate date, holidayenddate date, holidayname character varying(255), holidaystartdate date, holidaystatus character varying(255), holidaytype text, isallday boolean);