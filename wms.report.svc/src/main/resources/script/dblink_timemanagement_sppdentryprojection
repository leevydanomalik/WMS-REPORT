CREATE OR REPLACE VIEW public.trx_sppdentryprojection
AS SELECT result.sppd_obj_disc,
    result.id,
    result.createdby,
    result.createddate,
    result.recordid,
    result.updatedby,
    result.updateddate,
    result.employeeid,
    result.sppdcategory,
    result.sppdcurrency,
    result.sppddepartureplace,
    result.sppddestinationplace,
    result.sppddocumenturl,
    result.sppdenddate,
    result.sppdfacilities,
    result.sppdid,
    result.sppdnotes,
    result.sppdreason,
    result.sppdrequestby,
    result.sppdstartdate,
    result.sppdstatus,
    result.sppdtriptype,
    result.sppdtype
   FROM dblink('dbname=hcis-tm host=128.199.186.186 port=5432 user=hcis password=snGCcNt0w2JGHQBrP2kaTlQVk7HlxVjy'::text, 'SELECT * FROM trx_sppdentryprojection'::text) result(sppd_obj_disc character varying(50), id bigint, createdby character varying(255), createddate timestamp without time zone, recordid character varying(255), updatedby character varying(255), updateddate timestamp without time zone, employeeid text, sppdcategory text, sppdcurrency character varying(255), sppddepartureplace character varying(255), sppddestinationplace character varying(255), sppddocumenturl character varying(255), sppdenddate date, sppdfacilities text, sppdid character varying(255), sppdnotes text, sppdreason text, sppdrequestby text, sppdstartdate date, sppdstatus character varying(255), sppdtriptype text, sppdtype text);

-- Permissions

ALTER TABLE public.trx_sppdentryprojection OWNER TO hcis;
GRANT ALL ON TABLE public.trx_sppdentryprojection TO hcis;

