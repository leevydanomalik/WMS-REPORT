CREATE OR REPLACE VIEW public.trx_leaveentryprojection
AS SELECT result.leave_obj_disc,
    result.id,
    result.createdby,
    result.createddate,
    result.recordid,
    result.updatedby,
    result.updateddate,
    result.employeeid,
    result.leaveaddress,
    result.leavecategory,
    result.leavedocumenturl,
    result.leaveenddate,
    result.leaveid,
    result.leavenotes,
    result.leavereason,
    result.leavestartdate,
    result.leavestatus,
    result.leavetype,
    result.subtitutepersonid
   FROM dblink('dbname=hcis-tm host=128.199.186.186 port=5432 user=hcis password=snGCcNt0w2JGHQBrP2kaTlQVk7HlxVjy'::text, 'SELECT * FROM trx_leaveentryprojection'::text) result(leave_obj_disc character varying(50), id bigint, createdby character varying(255), createddate timestamp without time zone, recordid character varying(255), updatedby character varying(255), updateddate timestamp without time zone, employeeid text, leaveaddress character varying(255), leavecategory text, leavedocumenturl character varying(255), leaveenddate date, leaveid character varying(255), leavenotes text, leavereason text, leavestartdate date, leavestatus character varying(255), leavetype text, subtitutepersonid text);

-- Permissions

ALTER TABLE public.trx_leaveentryprojection OWNER TO hcis;
GRANT ALL ON TABLE public.trx_leaveentryprojection TO hcis;

