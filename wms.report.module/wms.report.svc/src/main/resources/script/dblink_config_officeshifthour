SELECT
    result.id,
    result.calendartype,
    result.esid,
    result.shiftcode,
    result.shiftenddate,
    result.shiftendtime,
    result.createdby,
    result.createddate,
    result.modifiedby,
    result.modifieddate,
    result.shiftleaderid,
    result.shiftstartdate,
    result.shiftstarttime,
    result.shiftstatus,
    result.shifttype
FROM dblink('dbname=hcis-configuration host=128.199.186.186 port=5432 user=hcis password=snGCcNt0w2JGHQBrP2kaTlQVk7HlxVjy'::text, 'SELECT * FROM mst_officeshifthour'::text) result(id bigint, calendartype text, esid character varying(255), shiftcode character varying(255), shiftenddate date, shiftendtime timestamp without time zone, createdby character varying(255), createddate timestamp without time zone, modifiedby character varying(255), modifieddate timestamp without time zone, shiftleaderid character varying(255), shiftstartdate date, shiftstarttime timestamp without time zone, shiftstatus character varying(255), shifttype text);