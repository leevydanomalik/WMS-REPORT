CREATE OR REPLACE VIEW public.trx_overtimeentryprojection
AS SELECT result.overtime_obj_disc,
    result.id,
    result.createdby,
    result.createddate,
    result.recordid,
    result.updatedby,
    result.updateddate,
    result.employeeid,
    result.overtimedate,
    result.overtimedocumenturl,
    result.overtimeenddate,
    result.overtimeid,
    result.overtimenotes,
    result.overtimestartdate,
    result.overtimestatus,
    result.overtimetaskdescription,
    result.overtimetype
   FROM dblink('dbname=hcis-tm host=128.199.186.186 port=5432 user=hcis password=snGCcNt0w2JGHQBrP2kaTlQVk7HlxVjy'::text, 'SELECT * FROM trx_overtimeentryprojection'::text) result(overtime_obj_disc character varying(50), id bigint, createdby character varying(255), createddate timestamp without time zone, recordid character varying(255), updatedby character varying(255), updateddate timestamp without time zone, employeeid text, overtimedate date, overtimedocumenturl character varying(255), overtimeenddate timestamp without time zone, overtimeid character varying(255), overtimenotes text, overtimestartdate timestamp without time zone, overtimestatus character varying(255), overtimetaskdescription text, overtimetype text);

-- Permissions

ALTER TABLE public.trx_overtimeentryprojection OWNER TO hcis;
GRANT ALL ON TABLE public.trx_overtimeentryprojection TO hcis;

